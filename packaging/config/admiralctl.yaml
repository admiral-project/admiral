# Admiral CLI configuration
# Installed by the admiralctl RPM to /etc/admiralctl/config.yaml
# Users may override via ~/.config/admiralctl/config.yaml

server_url: https://127.0.0.1:8080
token: __REQUIRED__
ca_cert_file: /etc/admiral/tls/ca.pem
