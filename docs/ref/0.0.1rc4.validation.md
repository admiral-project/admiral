# Admiral 0.0.1rc4 EL10 release validation

- **Result:** PASS — the corrected shared set with Fleet `-6` passed all six clean single-node and multi-node scenarios. Each single-node scenario passed the full WordPress lifecycle and inter-instance restore; each multi-node scenario passed role installation, peer health, and a WordPress workload on Worker.
- **Completed:** 2026-09-23 UTC.
- **Candidate:** one shared set of six Admiral RPMs, listed with SHA-256 below.
- **Validation branch:** `fix/release-validation-el10-installer-audit`.
- **Source commit:** `bd87dfbb82821fed1b777de771b1e4a61880bb7e`.
- **Installer:** `https://raw.githubusercontent.com/admiral-project/admiral/fix/release-validation-el10-installer-audit/scripts/install.sh`.
- **Package repo:** `http://192.168.122.1/el10/x86_64/`, configured as `admiral-local`, enabled with priority `1` on the validation VMs.

## Required matrix

| Distribution | Single-node | Multi-node (admin + portal + worker) |
|---|---|---|
| CentOS Stream 10 | PASS | PASS |
| AlmaLinux 10.2 | PASS | PASS |
| Rocky Linux 10.2 | PASS | PASS |

The earlier six-scenario installation result was for the historical `fleet-5` set and does not count toward the corrected candidate's release verdict. See the historical-candidate section below.

All three corrected-candidate single-node installs completed successfully with `failed=0` and `unreachable=0`; each also passed the full Golden lifecycle. The multi-node recaps below belong to the historical `fleet-5` candidate only and are diagnostic history, not corrected-candidate results. In those clusters, the installer ran on the admin node, and portal and worker were installed from that configured admin.

| Distribution | Admin recap | Portal recap | Worker recap |
|---|---|---|---|
| CentOS Stream 10 | `ok=200 changed=99 failed=0` | `ok=195 changed=87 failed=0` | `ok=148 changed=57 failed=0` |
| AlmaLinux 10.2 | `ok=201 changed=99 failed=0` | `ok=196 changed=87 failed=0` | `ok=149 changed=57 failed=0` |
| Rocky Linux 10.2 | `ok=201 changed=99 failed=0` | `ok=196 changed=88 failed=0` | `ok=149 changed=58 failed=0` |

In every historical multi-node cluster, both spokes registered as `active`, `healthy`, and `available`; both WireGuard peers handshook. Harbor health returned HTTP 200. Workers had active Fleet service and a listener on their WireGuard address at port 9099; rootless Podman reported `true`. Expected services were active, `systemctl --failed` was empty, SELinux was `Enforcing`, and only SSH was exposed in each spoke's public firewall zone. The temporary bootstrap SSH key was rejected by both spokes after install, then its private/public files and temporary known-hosts file were removed from the admin.

Corrected-candidate Rocky Linux 10.2 multi-node recap: Admin `ok=201 changed=99 unreachable=0 failed=0`; Portal `ok=196 changed=87 unreachable=0 failed=0`; Worker `ok=149 changed=57 unreachable=0 failed=0`. All six candidate RPMs were installed from `admiral-local` across the three roles. The nodes registered `active`, `healthy`, and `available`, with WireGuard handshakes to both spokes; Harbor health returned HTTP 200. The worker passed `admiralctl nodes ready`, ran rootless Podman, and hosted a WordPress instance that returned HTTP 200 from Admin over WireGuard. Deprovision succeeded and left no workload containers, volumes, systemd units, or instance directory. The bootstrap key was revoked, and each spoke's generated `admiral-ssh` key worked from Admin.

Corrected-candidate AlmaLinux 10.2 multi-node recap: Admin `ok=201 changed=99 unreachable=0 failed=0`; Portal `ok=196 changed=86 unreachable=0 failed=0`; Worker `ok=149 changed=56 unreachable=0 failed=0`. All six candidate RPMs were installed from `admiral-local` across the three roles. Caddy `2.10.2-9.el10_2` came from EPEL on Admin. Portal and Worker registered `active`, `healthy`, and `available`; both WireGuard peers handshook, and Harbor health returned HTTP 200. Worker passed `admiralctl nodes ready` and rootless Podman. A WordPress workload on Worker completed setup, returned HTTP 200 from Admin over WireGuard, then deprovisioned successfully; no workload containers, volumes, systemd units, or instance directory remained. The temporary bootstrap key was rejected by both spokes, and both generated `admiral-ssh` keys worked from Admin.

Corrected-candidate CentOS Stream 10 multi-node recap: Admin `ok=200 changed=99 unreachable=0 failed=0`; Portal `ok=195 changed=87 unreachable=0 failed=0`; Worker `ok=148 changed=57 unreachable=0 failed=0`. All six candidate RPMs were installed from `admiral-local` across the three roles. Caddy `2.10.2-9.el10_3` came from EPEL on Admin. Portal and Worker registered `active`, `healthy`, and `available`; both WireGuard peers handshook, and Harbor health returned HTTP 200. Worker passed `admiralctl nodes ready` and rootless Podman. A WordPress workload on Worker completed setup and returned HTTP 200 from Admin over WireGuard, then deprovisioned successfully; no workload containers, volumes, systemd units, or instance directory remained. The temporary bootstrap key was rejected by both spokes, and both generated `admiral-ssh` keys worked from Admin.

Corrected-candidate single-node recaps were `ok=229 changed=102 unreachable=0 failed=0` on CentOS Stream 10 and `ok=230 changed=102 unreachable=0 failed=0` on AlmaLinux 10.2 and Rocky Linux 10.2. The expected services were active, SELinux was `Enforcing`, no failed systemd units remained, and each installer's Harbor authentication check passed. Reconciliation recaps were CentOS `ok=195 changed=13`, AlmaLinux `ok=196 changed=12`, and Rocky `ok=196 changed=12`; all had `unreachable=0 failed=0`.

## Shared RPM set

All six corrected-candidate packages installed together from `admiral-local` on each of the three single-node VMs. In all three corrected-candidate multi-node scenarios, DNF selected all six packages from `admiral-local` across Admin (`admiral-common`, `admiral-flagship`, `admiral-fleet`, `admiral-harbor`, `admiralctl`, `admirald`), Portal (`admiral-common`, `admiral-harbor`), and Worker (`admiral-common`, `admiral-fleet`). DNF resolved remaining dependencies from configured distribution repositories.

| RPM | NEVRA | SHA-256 |
|---|---|---|
| `admiral-common` | `admiral-common-0.0.1rc4-7.el10.noarch` | `c18420033a742ced37e9cae373d910d169cc0b4c4f13ec1ce98991eb34deb29e` |
| `admiral-flagship` | `admiral-flagship-0.0.1rc4-5.el10.noarch` | `9894e7c914b2c5766d7f69377e8f3ec4283228c295c7d15597e61814496b6fe3` |
| `admiral-fleet` | `admiral-fleet-0.0.1rc4-6.el10.x86_64` | `bae8106530b0e20e06330849110972bbef7edee67bef2cf3ad31de5dcc480276` |
| `admiralctl` | `admiralctl-0.0.1rc4-5.el10.x86_64` | `db638ec994904b72b4805ff2572d6c307f7eec20f0301a5c0b95aff830eff347` |
| `admirald` | `admirald-0.0.1rc4-5.el10.x86_64` | `3e97a6b93b9568a676435b0a61c7694ac6da91a4c7a5800d500c67a9d0c742f2` |
| `admiral-harbor` | `admiral-harbor-0.0.1rc4-5.el10.noarch` | `b90efedff14fca22d1d925d35a665c065683efc5804c54b3d6d0e8acd5e72394` |

## Caddy and repositories

Caddy from EPEL is sufficient for Project Admiral. On all three corrected-candidate single-node VMs, DNF selected Caddy from EPEL: `2.10.2-9.el10_3` on CentOS Stream 10 and `2.10.2-9.el10_2` on AlmaLinux 10.2 and Rocky Linux 10.2. The corrected-candidate CentOS Stream multi-node Admin selected `2.10.2-9.el10_3`; the Rocky and AlmaLinux multi-node Admin nodes selected `2.10.2-9.el10_2`. No Caddy COPR repository file was present on any node in the three multi-node clusters; only the GPG-checked Admiral COPR remained enabled alongside EPEL and distribution repositories.

## Source and build checks

Component commits at validation time:

- flagship `c21c81b7b95d7f44d8c878f6067ff178af065feb`
- fleet `1b047c66abdf305ac471f924eda1f8112b5d0335`
- harbor `fc7dc38bbdb741fce98607b4e8aa34aa432968ac`
- admiralctl `98bf7ff26bdec0313320af1a885f932389feba13`
- admirald `e94d82011cca4197c58f0c294aa1c391b58b3c00`

`python3 scripts/validate-release-refs.py` passed. `make rpm-admiral` passed, as did the Flagship suite (266 tests), Harbor suite (321 tests), and `python3 scripts/test_installer_modes.py` (61 tests).

## Notes and scope

- The CentOS Stream 10 multi-node clones received `dnf update --refresh -y` before installation (35 packages, about 109 MB), rebooted into kernel `6.12.0-269.el10.x86_64`, and `dnf check-update --refresh` found no pending packages. AlmaLinux and Rocky validation also used freshly updated disposable clones. All three official GenericCloud base image SHA-256 values match the recorded official checksum manifest.
- Rocky Linux 10.2 multi-node clones received `dnf update -y --refresh` (244 packages per node) and rebooted into kernel `6.12.0-211.56.1.el10_2.0.1.x86_64`. Rocky's Portal mirrorlist initially mixed mismatched `sudo` package versions; its retry used temporary DNF command-line base URLs to the official Rocky 10.2 BaseOS and AppStream mirrors. This made no persistent repository changes.
- AlmaLinux 10.2 multi-node clones received `dnf update -y --refresh` and rebooted into kernel `6.12.0-211.56.1.el10_2.x86_64`; a subsequent `dnf check-update --refresh` found no pending packages on any role.
- AlmaLinux EPEL had a transient mirror transfer failure for the Caddy RPM; DNF retried and installed it successfully. CentOS EPEL key import emitted a non-fatal SHA-1 subkey policy warning; package installation succeeded.
- The local repository now serves only the corrected six candidate RPMs. Older RC4 RPMs were moved outside its document root, and the repository metadata and SHA256SUMS were regenerated. A fresh DNF repoquery returns Fleet `-6` and exactly six Admiral RPMs.
- All corrected-candidate single-node install, reconciliation/security, WordPress lifecycle, inter-instance database and volume restore, and cleanup gates passed on CentOS Stream 10, AlmaLinux 10.2, and Rocky Linux 10.2. All three corrected-candidate multi-node role installations, peer/workload checks, and cleanup gates also passed. The six-scenario Tier 1 release validation matrix passed.
- Validation VMs are shut down between test steps. The local RPM HTTP service remains available, and the candidate VMs retain the enabled local repository configuration.
- Development notes and findings are tracked in [issue #102](https://github.com/admiral-project/admiral/issues/102). Related fixes remain tracked in [#121](https://github.com/admiral-project/admiral/issues/121), [#122](https://github.com/admiral-project/admiral/issues/122), [#123](https://github.com/admiral-project/admiral/issues/123), and [#124](https://github.com/admiral-project/admiral/issues/124). Those issues and PR #117 remain open.

## Historical candidate: Admiral Fleet `0.0.1rc4-5`

The previous set used Fleet source commit `059d3503f3e1369eef5c0497f0206bee973aafba` and RPM SHA-256 `a357f00f6a7a319115fd888702db3a0e6c7ed5cd48e87aeb7693301008241204`. Its six clean installer scenarios completed, but Rocky single-node WordPress image update failed. Operation `op_6c5c3c62e30b095b` was killed after about 57 seconds while pulling `wordpress:6.8.1`; no kernel or cgroup OOM event was present. The package source had not incorporated the already tested ten-minute pull timeout from Fleet PR #87, commit `1b047c66`.

After pinning the corrected Fleet source, the new `fleet-6` RPM passed `make rpm-admiral-fleet` and its `%check`. A focused retry on the previously used Rocky VM installed Fleet `-6`, cleared the `wordpress:6.8.1` image cache, then completed restart operation `op_80875a83306f777e` in 44 seconds. HTTP returned 301, `need_restarting=false`, and the running image ID changed from `5bd67543f050fc873792b3bb092c9db43eaaa3d58f1d51dc633e0fd1fe0636d6` to `36e2490fa1a957f3cee615693157139c64b69951dcc7bfd48ee4fd6d03750a31`. This focused check is diagnostic only; final acceptance requires the clean matrix with the corrected shared set.
